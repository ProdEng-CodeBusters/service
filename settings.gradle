pluginManagement {
  plugins {
  }
  resolutionStrategy {
  }
  repositories {
      maven { url "https://plugins.gradle.org/m2/org/springframework/boot/" }
      gradlePluginPortal()
  }
}
rootProject.name = 'hello'
